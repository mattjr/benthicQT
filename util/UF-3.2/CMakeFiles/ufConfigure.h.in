/* 
 * Here is where system computed values get stored.
 * These values should only change when the target compile platform changes.
 */

#cmakedefine UF_BUILD_SHARED_LIBS
#ifndef UF_BUILD_SHARED_LIBS
#define UF_STATIC
#endif

